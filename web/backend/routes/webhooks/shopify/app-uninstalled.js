import lifecycleHooks from "../../../lifecycle-hooks/index.js";

export function setupAppUninstalledWebhook(path){// TODO: fix
  // Shopify.Webhooks.Registry.addHandler("APP_UNINSTALLED", {
  //   path,
  //   webhookHandler: async (_topic, shop, _body) => {
  //     // Recommended: delete all data associated with the uninstalled shop in your db here
  //     lifecycleHooks.uninstalled({shop})
  //   },
  // });
}

